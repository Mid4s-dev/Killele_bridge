# Kilele Bridge — Python dependencies (pinned for reproducible builds)

# Web framework & server
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0          # Heroku process manager; spawns uvicorn workers

# Database
sqlalchemy==2.0.31
pymysql==1.1.1            # MySQL driver used by SQLAlchemy
cryptography==42.0.8      # pymysql SSL support
alembic==1.13.2           # schema migrations

# Config & validation
pydantic==2.8.2
pydantic-settings==2.3.4
python-dotenv==1.0.1

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# File uploads
python-multipart==0.0.9

# HTTP client (IntaSend SDK dependency)
httpx==0.27.0

# Payments
intasend-python==1.0.4
